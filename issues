types are not unique - introducing a type with the same name as in intent typeset will lead to collision (namespaces are not respected)

matchers for grouping do not work?

import keyword for importing already existing modules would be helpfull

dynamic types and basic types are not composable